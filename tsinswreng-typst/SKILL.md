---

name: tsinswreng-typst

description: 編寫typst(.typ)代碼時使用此skill

---

## 標題寫法

- 使用 `auto-heading` 庫;
- **不要**把標題編號硬編碼到標題文本中！

## 加粗寫法

- `*`號包裹的內容是加粗文本。注意別和markdown搞混了! 是單個`*`包裹

## 段落寫法

建議使用自定義`P`函數放段落。

## 標點後換行

在以下標點符號後換行:

- ，
- 。
- ；
- ：
- ！
- ？

不論全角半角都要換。

.typ中的單個換行會被視爲空格， 兩個及以上的連續換行纔是分段

好處:

- 方便閱讀、
- 方便diff。

## 列表寫法

列表中一個元素內的文本換行時、要用`#[]`內容塊包裹

## Todo處理

格式是

```typst
#Todo[Todo描述][
	正文
]
```

你處理`#Todo`的時候, 處理完成之後要保留`#Todo`標記, 只能改正文, 不要改Todo描述。 待用戶確認無誤後, 由用戶自行移除待辦標記。

## 綜合示例

```typ
//自動標題庫
#import "@preview/tsinswreng-auto-heading:0.1.0": auto-heading
#let H = auto-heading
//自製段落
#let P(C) = {C}
#let Todo(Title, Body)={}
#H[Typst示例][
	#H[北風和太陽][
		#P[
			北風和太陽是好朋友。//標點後斷行
			可有一天，
			他們為了爭論誰的本領大而吵起來了。
		]
		
		#P[
		北風說：
		“我的本領大！
		我只要輕輕一口氣，
		人們全都凍得發抖。
		”太陽說：
		“我的本領大！
		我只要稍稍一用力，
		人們全都熱得出汗。
		”他們爭來爭去，
		誰也說服不了誰。
		]
	]
	#H[這是第二個二級標題][
有序列表:
+ a不換行文字
+ #[b換行文字，
	必須用內容塊包裹
]

無序列表:
就是把有序列表的`+`號改成`-`號。
注意所有列表/標題 序號都必須用讓typst自動計數，
禁止硬編碼！

	]
]
```
